{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block page_content %}
    <h1>Category index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>Created</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.label }}</td>
                <td>{{ category.created ? category.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('user_app_category_show', {'id': category.id}) }}">show</a>
                    <a href="{{ path('user_app_category_edit', {'id': category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_category_new') }}">Create new</a>
{% endblock %}
