<form method="post" action="{{ path('superadmin_category_delete', {'id': category.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn"><icon name="trash" display="solid" color="white" size="4"></icon></button>
</form>
